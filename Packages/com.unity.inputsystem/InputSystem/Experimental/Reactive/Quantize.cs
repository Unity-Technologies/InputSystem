namespace UnityEngine.InputSystem.Experimental
{
    // TODO Allow to quantize input, e.g. convert Vector2 into directional d-pad like input or generate quantized selection for e.g. radial menu conveniently.
    public class Quantize
    {
        // We may generate intervals instead of doing angle based computation if faster.
        // Double press e.g. dpad or direction is common operation.
    }
}
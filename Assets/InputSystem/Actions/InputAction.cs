namespace ISX
{
    //every action is part of a set
    //if it's a "lose" action, creates a hidden set internally
    //actions act as storage containers for the actions they contain
    public class InputAction
    {
        public InputAction()
        {
            
        }
    }
}

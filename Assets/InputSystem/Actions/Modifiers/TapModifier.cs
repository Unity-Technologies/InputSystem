namespace ISX
{
    // Requires the triggering control to press and release in less than
    // the set duration (which defaults to InputConfiguration.ClickTime).
    public class TapModifier
    {
        public float duration;
    }
}

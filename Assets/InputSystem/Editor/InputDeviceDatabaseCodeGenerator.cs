namespace ISX.Editor
{
    // Generates a wrapper around an InputDeviceDatabaseAsset which automatically
    // ensures that all the C# plugins are initialized.
    public static class InputDeviceDatabaseCodeGenerator
    {
    }
}

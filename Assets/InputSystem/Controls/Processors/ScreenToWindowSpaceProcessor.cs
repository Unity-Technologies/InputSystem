using UnityEngine;

namespace ISX.Processors
{
    /// <summary>
    /// Turns coordinates from screen space into the window space of the currently
    /// active window.
    /// </summary>
    public class ScreenToWindowSpaceProcessor : IInputProcessor<Vector2>
    {
        public Vector2 Process(Vector2 value)
        {
            throw new System.NotImplementedException();
        }
    }
}
